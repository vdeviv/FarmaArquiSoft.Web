@page
@model Microsoft.AspNetCore.Mvc.RazorPages.PageModel
@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="container py-5">
    <div class="card shadow-sm p-4 text-center">
        <h3 class="text-danger"><i class="bi bi-x-circle me-2"></i> Acceso denegado</h3>
        <p class="text-muted">No tienes permisos para ver este recurso.</p>
        <a asp-page="/Index" class="btn btn-outline-primary">Volver al inicio</a>
    </div>
</div>
